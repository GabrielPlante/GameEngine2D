#include "PhysicComponent.h"

namespace ian {

	/*
	void operator<<(std::vector<float>& vector, const PhysicComponent& physicComponent) {
		vector << physicComponent.position;
		vector << physicComponent.destination;
		vector.push_back(physicComponent.speed);
	}

	void operator>>(std::vector<float>& vector, PhysicComponent& physicComponent) {
		physicComponent.speed = vector.back();
		vector.pop_back();
		vector >> physicComponent.destination;
		vector >> physicComponent.position;
	}
	*/

}